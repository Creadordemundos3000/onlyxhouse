rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // ⚠️ ADVERTENCIA: Estas reglas permiten que CUALQUIERA suba archivos.
      // Son solo para desarrollo.
      allow read, write: if true;
    }
  }
}